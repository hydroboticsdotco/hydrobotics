// Web stub — Privy isn't used on web; the welcome screen falls back to demo
// connect, so this component is never rendered on web.
export function EmailLogin(_props: { onConnected: (address: string) => void }) {
  return null;
}
