// Privy required polyfills — must load before anything else.
import "fast-text-encoding";
import "react-native-get-random-values";
import "@ethersproject/shims";
// Then the Expo Router entry.
import "expo-router/entry";
